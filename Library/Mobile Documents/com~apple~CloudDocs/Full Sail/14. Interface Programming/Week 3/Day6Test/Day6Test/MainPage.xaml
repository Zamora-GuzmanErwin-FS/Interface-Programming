<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Day6Test.MainPage">

    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="IPG Day 6" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <CarouselView x:Name="monkeyCarousel">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <Frame CornerRadius="10" BorderColor="#0CC577">
                        <StackLayout>
                            <Image Source="{Binding MonkeyImage}" HorizontalOptions="Center" />
                            <Label Text="{Binding Name}" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" FontSize="Large" TextColor="#FFFFFF"/>
                            <Label Text="{Binding Region}" HorizontalOptions="FillAndExpand" HorizontalTextAlignment="Center" FontSize="Large" TextColor="#FFFFFF"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
    </StackLayout>

</ContentPage>
